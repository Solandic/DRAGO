import { getResolvedErrorSrc176Evid101 } from '@wix/bi-logger-os-data/v2';
import { withoutDefaults } from '../util/withoutDefaults';
export function reportGetResolvedErrorBi(errorHandler, params) {
  var _errorToShow$action;
  const {
    errorAccessor,
    errorResolution
  } = params;
  const {
    biLogger,
    commonErrorsMapState
  } = errorHandler;
  const {
    error
  } = errorAccessor;
  const {
    errorToShow,
    consumerResolvedError
  } = errorResolution ?? {};
  biLogger.report(withoutDefaults(getResolvedErrorSrc176Evid101)({
    session_id: error == null ? void 0 : error._errorHandlerSessionId,
    translated_message: errorToShow == null ? void 0 : errorToShow.message,
    action_text: errorToShow == null || (_errorToShow$action = errorToShow.action) == null ? void 0 : _errorToShow$action.text,
    is_consumer_resolved_error: consumerResolvedError != null,
    common_errors_load_failed: String(commonErrorsMapState.commonErrors == null)
  }));
}
//# sourceMappingURL=reportGetResolvedErrorBi.js.map
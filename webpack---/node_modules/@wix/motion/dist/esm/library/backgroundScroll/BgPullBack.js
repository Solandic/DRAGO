import { measureCompHeight } from './utils';
export default function create(options, dom) {
  const measures = {
    compHeight: 0
  };
  if (dom) {
    measureCompHeight(measures, dom);
  }
  const easing = 'linear';
  const {
    zoom = 50
  } = options.namedEffect;
  return [{
    ...options,
    easing,
    part: 'BG_MEDIA',
    startOffset: {
      name: 'cover',
      offset: {
        type: 'percentage',
        value: 0
      }
    },
    endOffset: {
      name: 'cover',
      offset: {
        type: 'percentage',
        value: 0
      }
    },
    get endOffsetAdd() {
      return `${measures.compHeight}px`;
    },
    keyframes: [{
      transform: `perspective(100px) translate3d(0px, -${zoom / 3 | 0}%, ${zoom}px)`
    }, {
      transform: 'perspective(100px) translate3d(0px, 0px, 0px)'
    }]
  }];
}
//# sourceMappingURL=BgPullBack.js.map